<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".addFood.AddFoodInfoActivity"></activity>
        <activity android:name=".addFood.AddFoodActivity" />
        <activity
            android:name=".weight.WeightActivity"
            android:label="Weight" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".diary.DiaryActivity"
            android:label="Diary" />
        <activity
            android:name=".macronutrients.MacronutrientsActivity"
            android:label="Macronutrients" />
        <activity
            android:name=".macronutrients.GoalsActivity"
            android:label="Goals" />
    </application>

</manifest>